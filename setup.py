from setuptools import find_packages, setup

setup(
    name='amaterasu',
    version='1.0.0',
    description='Framework ETL para transformação e análise de dados',
    packages=find_packages(),
    python_requires='>=3.12',
    install_requires=[],
)
